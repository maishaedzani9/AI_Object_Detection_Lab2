# Models

Place custom architecture definitions in this directory. Pretrained weight files are intentionally ignored by Git. The default `yolov8n.pt` checkpoint is downloaded automatically by Ultralytics on first use.
